# Optional — the site works without these (graceful fallback).
# Bets aggregator (free tier at https://the-odds-api.com):
ODDS_API_KEY=
# AI single-game take via Google Gemini:
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash   # optional override
